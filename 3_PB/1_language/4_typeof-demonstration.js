// typeof(value);
// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/typeof

    // Should be number
    typeof(50);

    // Should be string
    typeof('Hello');

    // Should be boolean
    typeof(true);

    // Should be undefined
    typeof(test);
