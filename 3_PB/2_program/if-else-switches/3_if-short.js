let testBool = true;

// Normales If Statement
if(testBool)
{
    console.log('(Boolean) Dies ist wahr');
}

// Einzeiliges If Statement
if(testBool) console.log('(Boolean) Dies ist wahr');

// Einzeiliges If Statement in weiterer Zeile.
if(testBool)
    console.log('(Boolean) Dies ist wahr');